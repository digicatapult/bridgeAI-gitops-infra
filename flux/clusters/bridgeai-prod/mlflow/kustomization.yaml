apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: mlflow
resources:
  - source.yaml
  - release.yaml
  - volumes.yaml
configMapGenerator:
  - name: mlflow-values
    files:
      - mlflow-values.yaml=values.yaml
generatorOptions:
  disableNameSuffixHash: true
